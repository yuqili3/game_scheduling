# 2026 羽毛球大乱斗 — 赛事模拟与排程系统 Spec

> Status: Draft v2
> Author: yuqi.li
> Created: 2026-07-20
> 数据来源:《建群三周年活动:2026"我们来打羽毛球"大乱斗》赛程赛制.pdf、2026大乱斗记分表.pdf

---

## 一、赛制理解(来自两份 PDF)

### 1.1 基本盘

- 参赛总人数 **64 人 = 48 男 + 16 女**,分 **8 支队伍**,每队 8 人(6 男 + 2 女)
- 每队设 1 名**男队长**;队伍由 6/12 随机抽签产生,队长固定
- 比赛日: 7/19(周日)5PM–10PM,Seattle Badminton Club

### 1.2 赛制:三轮排位循环赛

- 三轮两两对抗,胜者进胜区、败者进败区,所有队伍打满三轮,决出第 1–8 名
- 对阵逻辑(以抽签编号 G1–G8 为例):
  - **第一轮(初赛)**: 上半区 G1vG2、G3vG4;下半区 G5vG6、G7vG8
  - **第二轮(半决赛)**: 胜者组 G1vG3 / G5vG7,败者组 G2vG4 / G6vG8(以第一轮结果为准)
  - **第三轮(总决赛)**: 交叉争夺 1/2、3/4、5/6、7/8 名
- 每轮对抗 = **5 场双打**: 女双×1 + 男双×3 + 第五场盲抽局
  - 每场 21 分制、三局两胜;每场胜 1 积分,5 场中积分多者赢该轮(3 分即锁定胜利)
  - 女双: 两名女生全部出战
  - 男双 1/2/3: 队长排兵布阵,每位男生三场男双中最多出战 1 次,需提交出场顺序
  - **第五场盲抽局**: 第 1、2 轮为混双,第 3 轮为男双
    - R1(混双): 队长互抽对方 1 名女生 + 1 名非队长男生
    - R2(混双): 互抽 1 名 R1 未被抽中且非队长的男生 + R1 未被抽中的女生
    - R3(男双): 互抽 2 名前两轮均未被抽中且非队长的男生

### 1.3 退赛/缺席规则

- **赛前(抽签公布后至 7/18)**: 由候补名单依序递补。为保持实力平衡:
  - 普通队员缺席 → 从其余 7 支完整队伍各随机抽 1 名非队长成员,连同候补队员共 8 人重新抽签,随机分编入 8 支队伍
  - 队长缺席 → 队长直接指定一名组员为新队长,空出的名额按普通队员规则处理
- **比赛日(7/19)当天**:
  - 男队员伤/缺 → 队长从本队剩余男队员随机抽 1 人顶替(承担重复出赛)
  - 女队员伤/缺 → 主办方从自愿观众抽 1 名女生递补;无观众则从未在场上比赛的队伍中随机抽 1 名女生
  - 队长伤/缺 → 指定组员接替

### 1.4 场地模型

- 每 4 队(2 场对抗)共用 **5 块场地**,按错峰顺序约 70 分钟打完一轮:
  - 0–20min: G1vG2 的 WD/MD1/MD2/MD3 占 4 块场地,G3vG4 的 WD 占第 5 块
  - 20–40min: G1vG2 打 XD,G3vG4 的 MD1–3 上场
  - 40min 后: 决胜第三局与 G3vG4 的 XD 收尾
- 时间表节点(最晚时间,打得快可提前): 5:10 R1 → 6:45 R2 → 8:15 R3 → 9:30 结束

---

## 二、系统设计

Pipeline 总览:

```
Step 0 参数文件 → Step 2 初始人员表 → (抽签分队) → Step 3 退赛重抽(0~N 次)
                                                        ↓
             Step 5 实时展示网页 ← Step 4 比赛日排程与实时调度
```

### Step 0. 参数文件(config.yaml)

用 **YAML**(UTF-8,原生支持中文键名与注释,比 JSON 更适合人工编辑)。所有超参数集中于此,程序不得硬编码。示例:

```yaml
赛事:
  名称: 2026大乱斗
  比赛日期: 2026-07-19

人员:
  女队员数量: 16        # 编号 1-16
  男队员数量: 48        # 编号 17-64
  队伍数量: 8
  每队女生数: 2
  每队男生数: 6          # 含队长
  队长性别: 男

赛制:
  总轮数: 3
  每轮场次: 5            # 女双1 + 男双3 + 盲抽1
  获胜所需场次: 3        # 5场3胜
  每局分数: 21
  每场局数: 3            # 三局两胜
  盲抽局类型: [混双, 混双, 男双]   # 按轮次

场地:
  场地总数: 10           # 每4队5块 × 2组,待确认
  每组场地数: 5

时长模型:                 # 单位: 分钟,均为单局(1 game)预估
  女双单局时长: 10
  男双单局时长: 13
  混双单局时长: 13
  强强对话加时: 3         # 双方均为上一轮胜者时,每局加时
  换场间隔: 2             # 可选,场地清空/热身缓冲

随机:
  默认种子: 20260719
```

### Step 1. 时间线与事件流

比赛周期内的关键时间节点(写入 config 或单独 timeline 段):

| 日期 | 事件 |
|------|------|
| 6/10 | 报名开放 |
| 6/12 | 报名修改/取消截止;**抽签分队并公布** |
| 7/12 | Waiver 提交截止 |
| 6/12 – 7/18 | **退赛窗口**: 任何退赛触发 Step 3 重抽流程,可发生多次 |
| 7/19 | 比赛日: 现场缺席按 1.3 当天规则处理,排程由 Step 4 实时调整 |

要求: 每次人员变动都产生一份带日期戳的新 CSV(见 Step 3),历史版本全部保留,可完整回放"报名 → 抽签 → 若干次退赛 → 最终名单"的全过程。

### Step 2. 初始人员表

报名结束后建立初始人员表,**男女分成两份**:

- `players_female.csv`: 序号 **1–16**
- `players_male.csv`: 序号 **17–64**

字段: `序号, 姓名, 性别, 是否队长`。初始表**只固定 8 名男队长**(is_captain=true),其余人员不属于任何队伍;队伍归属由抽签程序产生,输出 `teams_YYYYMMDD.csv`(字段: `序号, 姓名, 性别, 角色[队长/队员], 队伍编号, 队长姓名`)。

抽签程序约束:

- 8 名队长分别落入 8 队
- 每队恰好 2 女 + 5 非队长男
- 接受随机种子输入,同种子结果可复现

### Step 3. 退赛重抽子程序(withdraw)

支持多次调用。每次调用:

**输入**: 当前最新的 `teams_*.csv`、退赛者序号(可多个)、候补者信息(如有)、**用户输入的随机种子**

**算法**(按 1.3 赛前规则):

1. 若退赛者是队长 → 提示指定新队长(输入新队长序号),原队长移出,继续按普通队员流程处理空缺
2. 从其余 7 支完整队伍中,各随机抽取 1 名非队长成员(与退赛者同性别,保持 6男2女 结构)
3. 该 7 人 + 候补递补者共 8 人重新随机抽签,分编入 8 支队伍的空缺位
4. 校验: 每队仍为 1 队长 + 5 男 + 2 女

**输出**: `teams_YYYYMMDD.csv`(以当天日期结尾;同日多次退赛可加序号 `teams_YYYYMMDD_2.csv`),并输出一份变更日志(谁动了、种子是多少),保证**任何一次重抽都可用相同种子重现**。

### Step 4. 比赛日场地排程与实时调度

**目标**: 场地利用率尽量高(最小化总时长 / 最大化并行度)。

**约束与规则**:

- 轮次依赖: 一轮打完才能安排下一轮;**放宽条件**——同一半区的两场对抗都结束后,该半区下一轮对阵即已确定,可提前开打(不必等另一半区)
- 每场对抗内的依赖: 盲抽局(第五场)人员在该轮赛前已抽定,但队员一人可能连打多场(男双+混双),排程需避免同一人同时出现在两块场地
- 单局时长预估(来自 config):
  - 女双 10 分钟/局,男双、混双 13 分钟/局
  - **强强对话加时**: 若对局双方(队伍)在上一轮均获胜,每局 +3 分钟
  - 每场 2–3 局(三局两胜),第三局是否发生按实际情况/模拟概率处理
- 输出: 类似 PDF 附录的场地×时间分配表(每 4 队 5 场地的错峰模板为基线),粒度到"某时刻-某场地-某对局-第几局"

**实时调整**: 比赛进行中,每当一局/一场实际结束时间录入,程序即:

1. 更新剩余对局的预计开始/结束时间
2. 若某场提前结束→立即把等待中的对局(含提前开打的下一轮半区)顶上空场地
3. 重新输出预期时间表,推送给 Step 5 的展示端

实现建议: 贪心 + 事件驱动模拟(每个"局结束"是一个事件,触发重排),无需复杂求解器。

### Step 5. 比赛日实时展示媒介

让现场所有人实时看到比赛与抽签结果。展示内容:

- 对阵图(bracket)与各队当前战绩/名次走向
- 每轮盲抽结果(第五场人员)公示
- 实时比分与各场地当前对局
- 下一场安排: 谁、几点、哪块场地(来自 Step 4 的实时输出)

技术形态(待定,见 Open Questions): 静态网页 + 定时刷新 JSON(可托管 GitHub Pages,录入端更新数据文件即可) vs. 轻量 Web App(如 Streamlit/Flask,支持现场录入比分)。

---

## 三、仓库结构(规划)

```
game_scheduling/
├── config.yaml              # Step 0 参数
├── spec.md
├── data/
│   ├── players_female.csv   # Step 2 初始表(女 1-16)
│   ├── players_male.csv     # Step 2 初始表(男 17-64)
│   └── teams_YYYYMMDD*.csv  # 抽签/每次退赛后的队伍快照
├── scripts/
│   ├── draw_teams.py        # 初始抽签
│   ├── withdraw.py          # Step 3 退赛重抽
│   ├── schedule.py          # Step 4 排程与实时调度
│   └── simulate.py          # 整体模拟(蒙特卡洛)
└── web/                     # Step 5 展示端
```

---

## 四、Open Questions

1. **场地总数**: PDF 说"每四队拥有五个场地",8 队是否即共 10 块?比赛日场馆实际可用几块?
2. **候补名单**: 是否维护候补名单?若退赛发生而无候补可递补,是允许某队缺 1 人(队长顶场)还是其他方案?
3. **展示端形态**: 倾向静态网页(零运维,手动更新数据)还是可现场录入比分的小 App?比分由谁录入(志愿者/各队队长)?
4. **"强强对话"判定粒度**: 加时 3 分钟按"队伍上一轮均获胜"判定,还是按"该对局双方选手上一轮各自的对局均获胜"判定?第一轮是否一律不加时?
5. **模拟需求**: simulate.py 是否需要给选手设实力值,跑蒙特卡洛看名次分布/时长分布?
