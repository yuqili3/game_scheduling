# 羽毛球赛事模拟与排程系统 Spec

> Status: v5(v3 = 2026 大乱斗需求已确认并完赛;v4 新增通用抽签 + 2026 趣味混双友谊赛环境;v5 = 趣味混双改为指定队长 + 固定席位 + 混双搭档抽签,真实报名名单入库)
> Author: yuqi.li
> Created: 2026-07-20 · Updated: 2026-09-25
> 数据来源:《建群三周年活动:2026"我们来打羽毛球"大乱斗》赛程赛制.pdf、2026大乱斗记分表.pdf、
> 《我们来打羽毛球-2026趣味混双友谊赛》赛程赛制.pdf

spec 按版本分文件保存、不原地覆盖: `spec.md` = v3(首届 2026 大乱斗,冻结),`spec_YYYYMMDD.md` = 之后每次
需求变更的完整快照(本文件 = 2026-09-25 版,上一版 `spec_20260923.md`)。第一、二节按首届赛事写成,是本系统的原始需求;**1.5 与
2.x 中标注"通用"的部分适用于所有赛事**。每个赛事一个 `data/<env>/` 目录(见第四节),代码共用。

---

## 一、赛制理解(来自两份 PDF)

### 1.1 基本盘

- 参赛总人数 **64 人 = 48 男 + 16 女**,分 **8 支队伍**,每队 8 人(6 男 + 2 女)
- 每队设 1 名**男队长**;队伍由 6/12 随机抽签产生,队长固定
- 比赛日: 7/19(周日)5PM–10PM,Seattle Badminton Club,**共 10 块场地**

### 1.2 赛制:三轮排位循环赛

- 三轮两两对抗,胜者进胜区、败者进败区,所有队伍打满三轮,决出第 1–8 名
- 对阵逻辑(以抽签编号 G1–G8 为例):
  - **第一轮(初赛)**: 上半区 G1vG2、G3vG4;下半区 G5vG6、G7vG8
  - **第二轮(半决赛)**: 胜者组(winners)G1vG3 / G5vG7,安慰组(**consolation**,不称 loser)G2vG4 / G6vG8(以第一轮结果为准)
  - **第三轮(总决赛)**: 交叉争夺 1/2、3/4、5/6、7/8 名
- 每轮对抗 = **5 场双打**: 女双×1 + 男双×3 + 第五场盲抽局
  - 每场 21 分制、三局两胜;每场胜 1 积分,5 场中积分多者赢该轮(3 分即锁定胜利)
  - 女双: 两名女生全部出战
  - 男双 1/2/3: 队长排兵布阵,每位男生三场男双中最多出战 1 次,需提交出场顺序
  - **第五场盲抽局**: 第 1、2 轮为混双,第 3 轮为男双
    - R1(混双): 队长互抽对方 1 名女生 + 1 名非队长男生
    - R2(混双): 互抽 1 名 R1 未被抽中且非队长的男生 + R1 未被抽中的女生
    - R3(男双): 互抽 2 名前两轮均未被抽中且非队长的男生

### 1.3 退赛/缺席规则

- **赛前(抽签公布后至 7/18)**: 候补递补(候补人员姓名由用户手工录入)。为保持实力平衡:
  - 普通队员缺席 → 从其余 7 支完整队伍各随机抽 1 名非队长成员,连同候补队员共 8 人重新抽签,随机分编入 8 支队伍
  - 队长缺席 → 队长直接指定一名组员为新队长,空出的名额按普通队员规则处理
- **比赛日(7/19)当天**: 若某队缺人:
  - 每一轮由该队**队长指定一名队员**顶替缺席者出战(承担重复出赛),且**第 1、2、3 轮的顶替队员不能重复**(程序需校验)
  - 为加快进程,**该队所有比赛改为 15 分制**(时长预估同步按比例缩短)

### 1.4 场地模型

- 10 块场地,每 4 队(2 场对抗)共用 5 块,按错峰顺序约 70 分钟打完一轮:
  - 0–20min: G1vG2 的 WD/MD1/MD2/MD3 占 4 块场地,G3vG4 的 WD 占第 5 块
  - 20–40min: G1vG2 打 XD,G3vG4 的 MD1–3 上场
  - 40min 后: 决胜第三局与 G3vG4 的 XD 收尾
- 时间表节点(最晚时间,打得快可提前): 5:10 R1 → 6:45 R2 → 8:15 R3 → 9:30 结束

### 1.5 第二届赛事: 2026 趣味混双友谊赛(env `xd2026`)

来源:《我们来打羽毛球-2026趣味混双友谊赛》赛程赛制.pdf。与首届的差异决定了抽签程序必须通用化:

| 项目 | 2026 大乱斗(prod) | 2026 趣味混双(xd2026) |
|------|------|------|
| 人数 | 64 = 48 男 + 16 女 | **24 = 12 男 + 12 女** |
| 队伍 | 8 队 × 8 人(1 男队长 + 5 男 + 2 女) | **6 队 × 4 人(1 男队长 + 1 男 + 2 女)**;6 名男队长由主办方指定且**队号固定**(1 DreamWu、2 Michael Chou、3 Zack Chen、4 Yingtong Chen、5 Zhihao Hu、6 Guoquan Feng),其余 6 男 + 12 女随机抽入 |
| 混双搭档 | 无(每轮队长排兵) | **抽签时一并确定**: 每队两对固定混双组合,标签 = 队伍字母 + 对号(1 队 = A,2 队 = B,…;第 1 对 = 队长 + 1 女,第 2 对 = 另一男 + 另一女,即 A1/A2、B1/B2 … F1/F2,与 PDF 对阵表一致),谁跟队长搭档由种子决定 |
| 抽签日 | 6/12 | **9/25(周五)**,报名修改/取消截止同日中午 12:00 |
| 退赛 | 6/12–7/18 可退,7+1 重抽 | **公布后不接受退赛/退款**;极端情况按候补名单顺序**直接递补**(不重抽) |
| 水平分散 | 无要求 | "继续合理分散不同水平的选手"(可选 `level` 字段分层抽) |
| 比赛日 | 7/19 5–10PM,10 块场地,三轮排位 | **10/17(周六)8–10PM,Redmond Badminton Club,4 块场地** |
| 赛制 | 每对抗 5 场(WD+MD×3+盲抽),21 分三局两胜 | **团队积分循环赛**: 每队 5 局、每局 2 场混双、15 分制单局、每胜一场 1 分;排名: 总积分 → 净胜分 → 五局总得分 |
| 当天缺席 | 队长指定顶替 + 15 分制 | 主办方从自愿观众随机抽取递补,或同队队友替补 |

比赛日的对阵表(PDF 附表):每队拆成两个固定混双组合 A1/A2,前 5 轮 4 块场地、后 5 轮 2 块场地,
每轮 10 分钟,总计 2 小时。**本版本只交付 xd2026 的赛前抽签(Step 2/3 通用化);循环赛排程与
App 列入 4.2 roadmap。**

---

## 二、系统设计

Pipeline 总览:

```
Step 0 参数文件 → Step 2 初始人员表 → (抽签分队) → Step 3 退赛重抽(0~N 次)
                                                        ↓
             Step 5 实时展示/录入 App ← Step 4 比赛日排程与实时调度
                        ↑ 3-4 名志愿者现场录入比分
```

### Step 0. 参数文件(config.yaml)

用 **YAML**,键名与取值一律英文,注释可用中文。所有超参数集中于此,程序不得硬编码:

```yaml
players:
  num_female: 16          # 编号 1-16(仅注释用途,程序以 CSV 为准)
  num_male: 48            # 编号 17-64(17-24 为队长)
  num_teams: 8
  team_composition:       # 【通用】每队结构 = 若干"槽位",按顺序发牌;有 captain 槽则队长恒在首位
    - {gender: M, count: 1, role: captain}
    - {gender: F, count: 2}
    - {gender: M, count: 5}
  balance_by_level: true  # 【通用】人员表有 level 列时按水平分层抽,保证各队每档人数均衡(±1)
  # 兼容旧键: 若无 team_composition,则由 females_per_team / males_per_team(含队长)/
  # captain_gender 推导出上面三槽结构

format:
  num_rounds: 3
  matches_per_round: 5    # 女双1 + 男双3 + 盲抽1
  matches_to_win: 3       # 5场3胜
  points_per_game: 21
  shorthanded_points: 15  # 缺人队伍全部对局改用的分制
  games_per_match: 3      # 三局两胜
  blind_match_types: [XD, XD, MD]   # 按轮次: 混双/混双/男双

courts:
  total: 10
  per_bank: 5             # 每 4 队一组共用一个半区

duration:                 # 分钟,21 分制单局预估
  wd_game_minutes: 10
  md_game_minutes: 13
  xd_game_minutes: 13
  strong_pair_bonus: 3            # 第一轮一律不加时
  margin_per_game_threshold: 6    # 平均每局净胜 ≥ 6 视为"强"
  margin_per_match_threshold: 12  # 或全场净胜 ≥ 12 视为"强"
  rest_minutes: 5                 # 连打两场之间最短休息
  changeover_minutes: 2
  scale_by_points: true           # 15分制时长 = 21分制 × 15/21

visual:
  court_colors:                   # 空闲绿/热身黄/三局红色系
    idle: "#4CAF50"
    warmup: "#FFC107"
    game1: "#FF8A80"
    game2: "#D32F2F"
    game3: "#AD1457"

broadcast:
  refresh_seconds: 5
  session_start: "17:10"
  volunteers:                     # 每人独立姓名/PIN/负责场地
    - {name: VolunteerA, pin: "1111", courts: [1, 2, 3]}
    - {name: VolunteerB, pin: "2222", courts: [4, 5]}
    - {name: VolunteerC, pin: "3333", courts: [6, 7, 8]}
    - {name: VolunteerD, pin: "4444", courts: [9, 10]}

random:
  default_seed: 20260719
```

### Step 1. 时间线与事件流

| 日期 | 事件 |
|------|------|
| 6/10 | 报名开放 |
| 6/12 | 报名修改/取消截止;**抽签分队并公布** |
| 7/12 | Waiver 提交截止 |
| 6/12 – 7/18 | **退赛窗口**: 任何退赛触发 Step 3 重抽流程,可发生多次 |
| 7/19 | 比赛日: 缺人按 1.3 当天规则(指定顶替 + 15 分制),排程实时调整 |

### Step 2. 初始人员表

报名结束后建立初始人员表,**男女分成两份**:

- `players_female.csv`: 序号 **1–16**
- `players_male.csv`: 序号 **17–64**

字段: `id, name, gender, is_captain`(全英文,同代码约束)。**出于隐私,仓库内的人员表一律使用名人(celebrity)占位姓名**——性别与结构和真实名单一一对应(16 女、48 男、其中 8 名男队长),真实姓名只在线下保存,比赛日本地部署时替换。初始表**只固定 8 名男队长**(is_captain=true);队伍归属由抽签程序产生,输出 `teams_YYYYMMDD.csv`(字段: `id, name, gender, level, role[captain/member], team_id, captain_name`,无队长赛事 captain_name 留空)。

**【通用】人员表格式**: `data/<env>/players*.csv`(习惯上仍分 `players_female.csv` / `players_male.csv`,程序读取目录下所有 `players*.csv`),必填列 `id, name, gender`;可选列 `is_captain`(缺省 false)、`level`(水平档位,如 A/B/C 或 1/2/3,留空 = 不分档)和 `fixed_team`(抽签前钉在该队号,见下)。

**【通用】抽签约束(Step 2 核心算法,`core/draw.py::draw_teams`)**:

- 输入: 人员表、`num_teams`、`team_composition`、种子。**队伍结构不写死在代码里**,首届是 `[1 男队长, 2 女, 5 男]`,趣味混双是 `[1 男队长, 1 男, 2 女]`(v4 曾为 `[2 男, 2 女]`),未来任何男女配比只改 config
- 每个槽位对应一个独立候选池(按 gender + role 过滤);**池大小必须恰好等于 `count × num_teams`**,多一人少一人都直接报错(不再静默丢人)
- 每个池内: 若启用 `balance_by_level` 且有 level,先按档位分层,每档内洗牌后拼接;再把队伍顺序洗牌一次,**轮转发牌**(第 k 张给第 k mod n 队)。效果: 同档人员被均匀撒到各队(每队每档人数差 ≤1),而具体谁到哪队、哪队多拿一个高档位都由种子决定
- 槽位按 config 顺序发牌,有 captain 槽时把它写在第一位即可保证队长恒为 `teams[tid][0]`
- **【通用】固定席位(`fixed_team` 列)**: 人员表可选列 `fixed_team`,填了队号的人在抽签前就钉在该队(典型用法: 主办方指定的队长及其队号)。发牌时这些人从候选池剔除、并扣减对应队伍在该槽位的名额;剩余名额按"每队缺几个"逐层轮转发牌(第一层给每个还缺人的队各发一张,再发第二层……),同一层内仍是洗过序的不同队伍,所以分档均衡的性质不变。约束: 固定席位的性别/角色必须匹配某个槽位、同一队同一槽位不能超额、队号必须在 1..num_teams 内,否则报错
- **【通用】混双搭档抽签(`assign_pairs: true`)**: 抽完队伍后,对每队把男生与女生随机配对(要求每队男女人数相等,否则报错),配对顺序由同一种子决定,有队长时**队长所在的组合记为第 1 对**。组合标签 = 队伍字母 + 对号: 队号 1..n 映射为 A..Z(`pair_label(tid, idx)`),所以 1 队是 A1/A2,2 队是 B1/B2,…,6 队是 F1/F2,与 PDF 对阵表的写法一致。结果存在 `TournamentState.pairs`(队号 → [[男 id, 女 id], …]),快照 CSV 增加 `pair` 列(写标签,如 `A1`/`B2`)。直接递补时候补者继承退赛者的搭档;退赛重抽后受影响的队伍用该事件的种子重新配对
- 随机数只来自 `random.Random(seed)`,候选池先排序再洗牌,与文件行序无关;同种子必得同结果
- **【通用】种子可以是整数,也可以是任意文本**(如现场喊出的一句话 `"周五晚上见"`)。规范化规则(`core/models.py::parse_seed`): 去首尾空白后,若整体是十进制整数字面量(可带负号)则按 int 处理,否则按 str 处理;空串报错。整数与"同样数字的字符串"因此永远落到同一种解释,不会出现 `20260925` 与 `"20260925"` 抽出两套名单;但 `007` 会被记为 `7`。字符串种子经 Python `random.Random(str)` 的 SHA-512 派生,与进程、`PYTHONHASHSEED` 无关,跨机器可复现。事件日志与 SQLite 原样保存类型(JSON 数字 / 字符串;SQLite `seed` 列为 INTEGER 亲和,非数字文本按 TEXT 存)。该规则同样适用于 Admin 页对阵抽签、队长页盲抽、`withdraw.py`、`rehearse.py`
- 事件 `initial_draw` 的 payload 记录当时的 `num_teams`、`team_composition` 与**人员表指纹**(`roster_fingerprint`: 全体 id/性别/角色/档位的哈希,不含姓名),重放时与 config 和当前 `players*.csv` 比对,不一致即报错——防止赛后改 config 或补填 level 导致重放出另一套名单;改姓名(占位名换真名)不影响指纹

### Step 3. 退赛重抽子程序(withdraw)

支持多次调用。每次调用:

**输入**: 当前最新 `teams_*.csv`、退赛者序号、**候补者姓名(用户录入)**、**随机种子(用户输入)**

**算法**(按 1.3 赛前规则):

1. 退赛者是队长 → 用户输入新队长序号,原队长移出,空缺按普通队员流程处理
2. 从其余 7 支完整队伍各随机抽 1 名非队长成员(与退赛者同性别,保持 6男2女 结构)
3. 该 7 人 + 候补者共 8 人重新随机抽签,分编入 8 队空缺位
4. 校验: 每队仍为 1 队长 + 5 男 + 2 女

**输出**: `teams_YYYYMMDD.csv`(同日多次则 `teams_YYYYMMDD_2.csv`)+ 变更日志(退赛者、候补者、种子),同种子完全可重现。

算法对 `num_teams` 与队伍结构是通用的("其余 7 队"实为"其余全部队伍"),队长继任者须为同队、同性别的非队长成员。

### Step 3b. 直接递补子程序(substitute,通用)

趣味混双等"公布后不接受退赛"的赛事,极端情况按候补名单顺序递补——**候补者直接占用退赛者的位置,不触发重抽,无随机性**:

**输入**: 退赛者序号、候补者姓名(用户录入);若退赛者是队长,另需同队继任者序号

**算法**: 候补者获得新 id(当前最大 id + 1)、继承退赛者的性别与档位,插入原位;结构校验同 Step 2。事件 `substitute_direct`(不带种子),快照与变更日志同 Step 3。

### Step 4. 比赛日场地排程与实时调度

**目标**: 场地利用率尽量高(最小化总时长)。

**约束与规则**:

- **模板对齐排程(非朴素贪心)**: 参照赛程 PDF 附录的场地分配表——每场对抗内女双与男双 1–3 的前两局作为连续块并行开打;**盲抽局在这 4 场各打完前两局(+休息)后即可上场,不等可能存在的第三局**;条件第三局如模板中的 "3rd, TBD" 一样推迟、见缝插针地填入空闲场地。相比朴素贪心,最坏情况完赛时间 23:32 → 22:23,利用率 58.4% → 71.3%
- 轮次依赖(按**对阵粒度**放宽): **只要下一轮某场对抗的两支队伍都打完了上一轮,该场对抗即可提前开打**,不必等半区或全轮结束
- 同一人不能同时出现在两块场地;**同一人连打两场之间必须留至少 5 分钟休息**(超参 `连续出场休息`,男双+混双兼项、缺人顶替时尤其要检查)
- 单局时长预估(全部来自 config):
  - 21 分制: 女双 10 分钟/局,男双、混双 13 分钟/局;15 分制按 15/21 比例缩放
  - **强强对话加时 +3 分钟/局**: 按**场上选手上一轮的表现**判定,**第一轮一律不加时**。一个组合(2 人)被视为"强"当满足其一: (a) 两人上一轮各自的对局均获胜;(b) 该组合上一场对局大比分获胜——平均每局净胜 ≥ 超参 `每局净胜阈值`(6 分/局),或全场累计净胜 ≥ 超参 `每场净胜阈值`(12 分/场)。**对阵双方组合都"强"时触发加时**(需要逐场记录每位选手的胜负与比分,数据模型见第五节)
  - 第三局是否发生: 预排程**按满时长预留**(排程是时长瓶颈,必须按最坏情况即三局全打来估计);实际 2:0 结束后重排,该时段自动释放
- 输出: 场地×时间分配表,粒度到"时刻-场地-对局-第几局"

**实时调整**: 每当志愿者录入一局比分,调度器即:

1. 记录该局结束,更新场上选手的个人胜负记录
2. 重估所有未完对局的开始/结束时间(含加时判定的更新)
3. 有场地空出→把就绪对局(含可提前开打的下一轮)顶上去
4. 新时间表即时推送到观众页

实现: 贪心 + 事件驱动重排,每次录入触发一次全量重算(规模小,毫秒级)。

### Step 5. 实时展示与录入 App

**形态: Streamlit App**,四个页面(观众 / 志愿者 / 队长 / Admin):

- **观众页**(只读,自动刷新):
  - **场地状态可视化**: 10 块场地的网格面板,颜色编码实时状态——🟩 绿色=无人使用、🟨 黄色=热身中、红色系=比赛中(浅红=第一局、深红=第二局、紫红=第三局),色值来自 config `可视化.场地颜色`;每块场地显示当前对局、选手与实时比分
  - **轮次/晋级可视化**: 整场比赛的对阵树(bracket)——三轮的胜者组/败者组走向图,已结束的对抗标注胜负与比分,未开始的显示预计开打时间,最终名次(1–8)随比赛进程实时点亮
  - 各轮盲抽结果公示、"下一场:谁/几点/哪块场地"
- **志愿者录入页**(按人鉴权): 供 **3–4 名志愿者并发录入**比分。**每名志愿者在 config 中登记姓名、独立 PIN 和负责的场地列表**;登录后只显示且只能编辑自己负责的场地,天然避免并发冲突,事件日志的 actor 与 court 字段记录录入者与场地,可追溯。**页面顶部嵌入与观众页相同的十场地五色示意图**,便于核对场上的人是否正确;**只有主持人在 Admin Live 页确认开打(match_started)后,该场的比分录入才开放**——之前显示"等待主持人确认";**每块场地卡片完整显示: 场地号、当前对阵双方队伍、场上 4 名选手姓名、下一局局号、当前分制(21/15)**——志愿者需与场上人员当面核对后再录入。**布局规则: 每提交一局,比分保留在页面上并出现下一局的全新输入框;打完的 match 连同比分继续留在该场地区域,直到本轮全部结束才清空翻页**
- **队长页**(按队 PIN 鉴权,config `broadcast.captain_pins`): 每轮三件事——① **提交本队名单**(女双/男双1-3,校验每位男生最多出战一场男双;有比分后锁定,改动走 Admin);② **种子盲抽**: 按规则由队长抽**对方**的第五场人选——输入一个随机种子,程序在符合规则的池内(性别构成、非队长、排除前几轮已被抽中者)确定性抽出,种子记入事件日志,同种子必得同结果;③ **缺席登记与每轮顶替指定**(三轮顶替不得重复,该队自动改 15 分制)
- **Admin 页**(独立 PIN,config `broadcast.admin_pin`),四个标签:
  - **Live 主持人控制台**: 包含观众页的全部内容(场地五色图、对阵树、排名、盲抽公示、下一场列表),外加**逐场确认流**——每块空闲场地显示下一场的宣读词(谁和谁、哪块场地、什么项目);主持人念完、亲眼看到选手上场后按下"确认开打"按钮,追加 `match_started` 事件(记录场地与时刻),该场在所有页面变为"进行中",随后等待志愿者录分;一块场地上有未打完的已确认比赛时不能再确认新的比赛(程序校验)
  - **对阵抽签(种子驱动)**: 输入随机种子,程序把 8 支队伍随机分配到 G1–G8;种子记入 `group_draw` 事件,同种子必得同分组
  - **Lineups 状态总览**(只读监控,不再单设盲抽/缺席录入): 按轮次列出每场对抗双方——队长是否已提交名单、名单是否合规(女双 2 人、男双每场 2 人且无人兼两场男双、性别正确,缺项逐条列出);盲抽是否完成及结果(含种子);各队缺席/顶替登记状态。录入动作本身在队长页完成,Admin 在此监督
  - **比分更正(override)**: 志愿者录错时,选择场次与局号填入正确比分——以补偿事件 `game_corrected` 追加,原始录入保留在日志中;若更正改变了"打满两局即分胜负"的判定,多余的后续局自动从派生状态中剔除

---

## 三、可复现 + 可广播的程序架构(核心规划)

### 3.1 核心原则: Event Sourcing(事件溯源)

**唯一事实来源是一份只追加(append-only)的事件日志**,任何状态(队伍名单、比分、排名、排程表)都是"把事件从头重放一遍"的纯函数计算结果:

```
状态 = replay(config.yaml, events.jsonl)
```

事件日志 `events.jsonl` 每行一个 JSON 事件,统一结构:

```json
{"seq": 42, "ts": "2026-07-19T17:23:05", "type": "game_finished", "actor": "VolunteerA",
 "payload": {"node": "R1-1", "slot": "MD1", "game": 2, "score": [21, 15]},
 "seed": null}
```

事件类型(覆盖全生命周期):

| 阶段 | 事件类型 | 是否带种子 |
|------|---------|-----------|
| 赛前 | initial_draw(payload 记录 num_teams + team_composition)/ assign_teams / withdraw_redraw / substitute_direct(直接递补,无随机) | 抽签/重抽必带;直接递补不带 |
| 比赛日 | group_draw / lineup_submit / blind_draw_result / match_started / game_finished / absence_registered / substitute_assigned | group_draw、blind_draw_result 必带 |
| 修正 | game_corrected(比分更正,补偿事件,原录入保留) | 否 |

**为什么这样保证可复现**:

- 所有随机性(抽签、重抽、盲抽)的种子都记录在事件里;重放时用同一种子调用同一纯函数,结果必然一致
- 录入错误不改历史——追加一条"更正"事件,重放时后者覆盖前者,审计轨迹完整
- `events.jsonl` + `config.yaml` 提交进 git,任何时刻 `git checkout` + 重放 = 完整还原当时全场状态
- 现有的 `teams_YYYYMMDD.csv` 快照文件仍然生成,但定位是**重放结果的导出物**(方便人看),不是事实来源

### 3.2 代码分层

```
game_scheduling/
├── data/<env>/               # 每个赛事一个自包含目录: prod(2026 大乱斗)/ sim(彩排)/ xd2026(趣味混双)
│   ├── config.yaml           # 该赛事全部超参数,含 players.team_composition
│   ├── events.jsonl          # 唯一事实来源(git 追踪)
│   ├── players*.csv          # 初始人员表(可选 is_captain / level 列)
│   └── teams_YYYYMMDD*.csv   # 导出的快照(重放产物)
├── core/                     # 纯函数层: 不碰文件、不碰时钟、不自产随机数
│   ├── models.py             # Player/Slot/TeamComposition/Event/TournamentState 数据类
│   ├── rules.py              # 赛制规则: 积分、晋级、排名 1-5-3-7-2-6-4-8
│   ├── draw.py               # 通用抽签(composition 驱动)/重抽/直接递补/盲抽,全部 (state, seed) -> result
│   ├── scheduler.py          # 排程: (state, config) -> 场地时间表
│   └── replay.py             # 事件重放: (config, [events]) -> TournamentState
├── cli/                      # 赛前命令行工具(写事件),均支持 --env 与 --dry-run
│   ├── draw_teams.py         # 初始抽签
│   ├── import_teams.py       # 导入线下已抽好的名单
│   ├── withdraw.py           # 退赛重抽(退赛者/候补/种子)
│   └── substitute.py         # 直接递补(无随机)
├── app/                      # Step 5 Streamlit
│   ├── Home.py               # 观众页
│   └── pages/录入.py          # 志愿者页(PIN)
└── tests/                    # 规则与重放的单元测试(黄金事件流回归)
```

关键设计约束:

- **core/ 全部是纯函数**: 随机数只接受外部传入的 seed(内部 `random.Random(seed)`,不用全局 random);时间只来自事件的 ts 字段,代码里禁止 `datetime.now()` 参与状态计算。这是可复现的根基,也让单元测试极简
- **写路径唯一**: 只有 CLI 和录入页能追加事件;追加时加文件锁(或经 SQLite,见 3.3),seq 单调递增
- **读路径无状态**: 观众页每次刷新 = 读事件 → 重放 → 渲染。64 人 3 轮总事件量 < 千条,全量重放毫秒级,不需要缓存和增量逻辑

### 3.3 广播方案(比赛日部署)

**架构: 一台笔记本跑 Streamlit,全场手机浏览器访问**

```
志愿者手机(3-4人) ─┐
                   ├─ 场馆 WiFi/热点 ─→ 笔记本 Streamlit (端口 8501)
观众手机(~64人) ───┘                      ↕
                                    SQLite(events 表) ←→ 定期导出 events.jsonl 进 git
```

- **并发录入**: 事件先落 SQLite(WAL 模式,天然支持多志愿者并发写,seq 用自增主键),`events.jsonl` 作为 SQLite 的定期导出与 git 备份。这样既有并发安全,又保留纯文本可审计
- **访问方式**: 笔记本在场馆 WiFi 下起服务,观众页生成二维码(指向 `http://<局域网IP>:8501`)贴在场边;无可用 WiFi 则笔记本开热点
- **兜底**: 若局域网不可用,备选 Streamlit Community Cloud(需公网);极端情况回退到投屏观众页
- **性能预估**: ~70 人每 5 秒轮询一次纯读页面,Streamlit 单机完全可承受;录入与展示同库不同页,互不阻塞

### 3.4 可复现性的验收标准

1. 给定同一 `config.yaml` + 同一 `events.jsonl`,任何机器上重放得到逐字节一致的排名、名单、排程表
2. 删掉所有 CSV 快照,仅凭事件日志能全部重新导出
3. 赛后能回答"第二轮开始时全场是什么状态"——取 seq ≤ 该时刻的事件前缀重放即可
4. 每一次抽签结果,用日志里记录的种子离线重跑 `draw.py` 能得到相同结果

### 3.5 实施顺序(里程碑)

| # | 交付物 | 内容 |
|---|--------|------|
| M1 | core + 赛前 CLI | 数据模型、config 加载、初始抽签、退赛重抽、事件日志与重放、单元测试 |
| M2 | 排程器 | 时长模型(逐人胜负/净胜分追踪、加时判定、15 分制缩放、5 分钟休息约束)、按对阵粒度提前开打、事件驱动重排 |
| M3 | Streamlit App | 观众页(场地色块面板 + bracket 晋级图)+ 志愿者录入页(独立 PIN/分管场地)+ 二维码,SQLite 并发写 |
| M4 | 演练 | 用模拟事件流跑一遍完整比赛日(dry-run),验证 3.4 的四条标准 |

---

## 四、环境分离与复用路线图

### 4.1 sim / prod 数据分离

- 数据按环境隔离在 `data/<env>/`,每个环境自包含: config.yaml、players_*.csv、
  events.jsonl、tournament.db、快照。`GS_ENV` 环境变量或 CLI `--env` 选择,默认 prod
- App 在非 prod 环境的每个页面顶部显示 🧪 SIMULATION 警示条
- 彩排驱动器 `cli/rehearse.py` 默认 sim,拒绝在 prod 运行(需 `--force-prod`)
- 复用方式: 未来的混双/女双/男双专项赛 = 新建一个环境目录,放入该赛事的
  人员表与赛制 config,代码零改动(除下述排程泛化)
- **已落地的第一个复用环境: `xd2026`**(2026 趣味混双友谊赛,见 1.5)。`config.yaml` 的
  `players.team_composition = [{M,1,captain},{M,1},{F,2}]`、`num_teams = 6`、`assign_pairs = true`;
  人员表 12 女(id 1–12 = 女生报名顺序)+ 12 男(id 13–24 = 男生报名顺序 1–12),**使用真实报名姓名**
  (2026-09-25 主办方决定,见决策表"名单隐私(xd2026)"),6 名指定队长 `is_captain=true` 且 `fixed_team` 填队号,
  `level` 列留空(报名表的自评等级不录入)。赛前流程只用三个通用 CLI:
  `draw_teams.py --env xd2026 --seed <种子>`(9/25 抽签)、`import_teams.py`(线下抽签导入)、
  `substitute.py`(候补直接递补);App 与排程器暂不支持该赛制,不要在 xd2026 环境启动

### 4.2 排程算法泛化(roadmap,本届不做)

- **xd2026 团队积分循环赛**(下一步):6 队单循环、每局 2 场混双(每队两个固定组合 A1/A2)、
  15 分单局、按 PDF 附表的 4 场 + 2 场两段排程、排名三级比较(积分/净胜分/总得分);
  组合的随机上场顺序仍走种子事件

当前排程器按本届赛制硬前提工作: 12 节点固定对阵树、每对抗 5 场、10 块场地分
2 个半区(每半区 5 块)。未来复用需要:

- **场地数 3/4/5/6 的小场馆模型**: 半区概念退化为单一场地池,错峰模板需按
  场地数重新推导(当前代码已把场地分组从硬编码改为按 `per_bank` 切块,
  不足两组时自动折叠成单组,作为过渡)
- **赛制参数化**: 对阵树(队伍数/轮数)、每轮场次构成(纯混双/纯女双等)由
  config 生成而非写死 12 节点
- **时长模型按项目重标定**(不同项目的单局时长与加时规则)

## 五、已确认的决策记录

| 问题 | 决策 |
|------|------|
| 场地总数 | 比赛日实际 10 块 |
| 展示端形态 | 支持录入比分的 App(Streamlit),现场 3–4 名志愿者并发录入;每名志愿者有独立姓名/PIN/负责场地 |
| 强强对话加时判定 | 组合"强" = 两人上一轮均获胜,或上一场净胜 ≥6 分/局(或 ≥12 分/场,均为超参);双方都强才加时;第一轮一律不加时 |
| 轮次依赖 | 按对阵粒度: 下一轮某场对抗的两队都打完上一轮即可提前开打 |
| 连续出场 | 同一人连打两场之间至少休息 5 分钟(超参) |
| 赛前退赛候补 | 候补人员姓名由用户录入,走 7+1 重抽流程 |
| 比赛日缺人 | 每轮队长指定一名队员顶替(三轮顶替者不能重复);该队所有比赛改 15 分制 |
| 场地可视化 | 绿=空闲、黄=热身、浅红/深红/紫红=第1/2/3局;另有三轮胜败走向 bracket 图 |
| 蒙特卡洛模拟 | 不需要 |
| 语言约束 | 代码/配置/数据文件零中文(标识符、键名、事件类型、取值全英文);中文只出现在 spec 与 README |
| 排程算法 | 模板对齐(参照 PDF 附录):盲抽局在前四场各打完两局+休息后即开打,条件第三局并行填空;非朴素贪心 |
| 第三局占位 | 未决出的第三局按满时长预留(时长瓶颈按最坏情况);2:0 结束后重排自动释放 |
| 志愿者页布局 | 每局提交后比分留在页面并开新输入框;打完的 match 保留展示,整轮结束才清空 |
| 比分更正 | Admin 专属 game_corrected 补偿事件;原始录入不删,更正后多余局自动剔除 |
| 队长页 | 名单提交 + 种子盲抽(抽对方、规则内确定性)+ 缺席/顶替登记;每队独立 PIN |
| 对阵抽签 | Admin 页种子驱动随机分组,种子入日志可复现 |
| Admin Lineups | 状态总览(名单提交/合规、盲抽状态与结果、缺席/顶替),录入在队长页,Admin 只监督 + 比分更正 |
| 命名 | 败者半区一律称 consolation(节点缩写 R2-CU/R2-CL),不使用 loser |
| 志愿者页示意图 | 顶部嵌入与观众页相同的十场地五色状态图,核对场上人员 |
| 主持人确认流 | Admin Live 控制台含观众页全部内容;每场需主持人宣读并按"确认开打"(match_started 事件,含场地)后才算进行中,志愿者才能录分;一场地同时只能有一场已确认未完比赛 |
| 排程与确认联动 | 已确认开打的比赛,其剩余各局在排程中钉死在所确认的场地上 |
| 录分前置校验 | game_finished 硬性要求该场双方人员已齐(名单/盲抽);match_started 同样要求——杜绝"单边盲抽打完还显示 TBD"一类脏数据 |
| 名单隐私 | 仓库内人员表使用 celebrity 占位姓名(16 女 + 48 男含 8 队长,结构与真实名单一致);真实姓名不入库 |
| 环境分离 | data/prod 与 data/sim 各自自包含;GS_ENV/--env 切换;非 prod 页面显 SIMULATION 警示;rehearse 默认 sim 且拒绝 prod |
| 复用规划 | 未来专项赛=新环境目录;3-6 块场地的排程泛化列入 roadmap(spec 4.2) |
| spec 版本管理 | spec 不原地改;每次需求变更另存 `spec_YYYYMMDD.md` 完整快照,`spec.md` 冻结为 v3 |
| 通用抽签 | 队伍结构由 config `players.team_composition`(槽位列表: gender/count/role)描述,不写死;池大小与 count×num_teams 不符直接报错;槽位顺序 = 发牌顺序,captain 槽放首位 |
| 水平分层 | 人员表可选 `level` 列 + config `balance_by_level`;每池按档分层洗牌后轮转发牌,各队每档人数差 ≤1,哪队多拿由种子决定 |
| 种子类型 | 所有种子入口(CLI `--seed`、Admin 对阵抽签、队长盲抽)接受整数或任意文本;`parse_seed` 统一规范化(纯整数字面量→int,否则→str),事件原样记录类型 |
| 抽签事件自描述 | `initial_draw` payload 记录 num_teams、team_composition 与人员表指纹(id/性别/角色/档位,不含姓名),重放时与 config 及当前 CSV 比对,不一致即报错 |
| 直接递补 | 新事件 `substitute_direct`(无种子)+ `cli/substitute.py`:候补者继承退赛者的性别/档位/位置,适用于"公布后不接受退赛"的赛事 |
| 固定席位 | 人员表 `fixed_team` 列把指定成员钉在指定队号(xd2026: 6 名指定男队长 = 1–6 队);发牌按各队剩余名额逐层轮转,指纹包含 fixed_team |
| 混双搭档 | config `players.assign_pairs: true` 时抽签同时为每队随机配出男女组合,队长组合 = 第 1 对;标签 = 队伍字母 + 对号(A1/A2、B1/B2 … F1/F2);pairs 入状态与快照,递补继承、重抽重配 |
| 名单隐私(xd2026) | 与首届不同,xd2026 人员表直接使用真实报名姓名(主办方 2026-09-25 决定);自评等级/备注不录入。仓库为公开 GitHub,如需收回改为占位名 + 线下映射即可 |
| xd2026 范围 | 本版只交付赛前抽签/导入/递补;循环赛排程与 App 不启动,列入 4.2 |
